performance_limiter() {
    if(this.initialized == undefined) {
        this.initialized = true;
        set_cycle_sleep_seconds(0.0001);
        set_inactive_sleep_seconds(0.001);
    }
}

performance_limiter performance_limiter_inst();
