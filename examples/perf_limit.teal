performance_limiter() {
    if(this.initialized == undefined) {
        this.initialized = true;
        set_cycle_sleep_seconds(0.005);
        set_inactive_sleep_seconds(0.01);
    }
}

performance_limiter performance_limiter_inst();
